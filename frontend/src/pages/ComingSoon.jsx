const ComingSoon = ({ title }) => (
  <div>
    <h1 className="text-2xl font-bold text-slate-800 mb-4">{title}</h1>
    <div className="bg-white rounded-lg shadow-sm p-8 text-center text-slate-400">
      This page follows the same pattern as Materials.jsx — build it next by copying that file
      and swapping the API endpoint and form fields.
    </div>
  </div>
);

export default ComingSoon;
